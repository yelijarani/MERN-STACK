JAVA SCRIPT:
------------
->Javascript is the scripting language as well as programming language


WHY WE NEED JS:
---------------
->to build dynamic web applications
->to add functionalities to the web applications
->to manipulate HTML nad CSS
->To active DOM (Document object mode)funmctionalities
->To fetch data from the server(API) and also to send data to server JS is required


DIFFERENCE BETWEEN JAVA AND JAVASCRIPT:
--------------------------------------
JAVA :                                                                     JAVA SCRIPT :

->java object oriented programming language           |          ->js is the scripting language as well as object programming language
->java will supports multithreading                   |          ->js will support single call stack
->java is the imperative nature                       |          ->js is the dynamically typed(in js no need to define data types  to the variable                                              |
->java is the strictly typed                          |          ->js is the weakly typed



TOKENS:
-------
  ->Tokens are the smallest unites/elements in the js program

  CLASSIFICATION:
  1.identifiers
    ->identifiers are the names given to js code
     ->In the place of identifiers we can't use keywords
    EX:variable names,class names and function names
    
  2.literals:
              literals are the values given to the js code
        TYPES:
        1.string literals
        2.numeric literals
  3.keywords:
         ->keywords are the predefine words which having it's meaning
         ->In JS total 66 keywords present
         EX:
           int,char,function,float,boolen,class,const,var,let,if,else,for,while,do,continue,switch,break,default,double,return,export,catch.exports,finally,try,catch,super,this,throw,package,public,void,null,import,get,set,abstract,assert,byte,constant,enum,extends,implement,target,delete...etc
      
  4.separators:
        ->separators are used to separate the givencode
        EX:[],{},(),;:,',",.,,,<>

  5.operators:
        ->OPERATORS ARE UESD TO THE perform operations on operations
        EX:10+20
    TYPES:
      1.assignment operators(=,+=,-=,*=,/=,%=)
      2.relational operators(>,>=,<,<=)
      3.logical operators(+,-,8,/,%,**)
      4.unary operators(&,|,^)
      7.ternary operators     (condition ? expr1 :expr2)
      8.shift operators       (>> , <<)
      9.ccomparision operators (== , === , !=)
           6.comments
                 ->comments are used to comment the given code

                 types:
                       1.single line //
                       2.multi line comment /*

DataTypes in JS:
---------------
      ->DataTypes represents the what type of data we storing inn variables           
                  1.Primitive DataTypes  (immutable)
                       1.number
                       2.string
                       3.boolean
                       4.null
                       5.underline
                       6.symbol
                       7.bigInt
                  2.Non-Primitive DataTypes (mutable, object de-referencing)        
                       1.Arrays
                       2.Class type
                       3.objects
                       4.function              

variables
---------
     ->variables are the named memory location used to store data or values and it can change N number of times during execution

       str="javascript"
       str=10

1.variable declaration
2.variable initialization
3.variable utilization
4.variable re-initialization
5.variable re-declaration
IN JS WE CAN DEFINE VARIABLE IN 3 WAYS:
--------------------------------------
1.LET:
   ->let variable acts like a local scop/block scop
   ->variable declaration and initialization in same/different line is possible
   ->re-initialization is possible but re-declaration is not possible
   ->we can't hoist the let variables
 2.var:
           ->let variables acts like a global scop/block scop (because var present in windows object)
           ->variable declaration and initialization in  same/different line is possible
           ->re-initialization and re-declaration is possible
           ->we can hoist the var variables
3.const:
           ->const variables acts like like a final (because once initialization values we cant modify data/values)
           ->variable declaration and initialization in  same is possible but different lines is not possible
           ->re-initialization and re-declaration is not possible
           ->we cant hoist the const variable
                







HOISTING:
---------
   ->utilizing the variable before declaration
   ->hoisting is possible only through 'var' variable

STRINGS:
--------
->string are group of characters
->we can declare string 3 WAYS
  1.""
  2.''
  3.``
->template string & string interpolation


SRTRING INBUILT FUNCTIONS:
-------------------------



ARRAYS:
------
->Arrays arevlinear data structure used to homogeneous types of data
->Array size is not found

WAY TO DECLARE ARRAYS:
----------------------
1.literal WAY
2.using new keywords (array object)


FUNCTION/METHODS:
---------------
 ->functions is the block of the statements which will get execute when ever we called or invoke
 ->using functions we can increase code readability and code reuseability

 Basically we have 2 types functions:
  ----------------------------------
 1.user define functions
   ->Any functions that is declared by user or programmer as per there requirement

2.Predefine functions
   ->predefine functions  means inbuilt functions
   
   JS FUNCTIONS types
   -----------------
    1.Anonymes function
    2.Normal function/named function
    3.Functional expression
    4.Immediate invoking function/self calling function
    5.arrow function
    6.Nested function
    7.Higher order function:
           ->Any function that will be accept another parameter
    8.Callback function:
           ->Any functions passing as value to the higher order function
           
    9.Generator function

    OBJECTS:
    -------
      ->they are real time entities which are used to store data in the form ley and value pair

      1.literal way
      2.using keyword(create for object class)

DOM(Document object model)
------------------------------
              ->DOM are used to manipulate HTML and CSS 
              ->To build dynamic web pages


BOM(Browser object model)
--------------------------
         ->is the predefined inbuilt object present in every Browser
         ->it contains windows object


we can apply events in three different ways:
---------------------------------------------
  1.attribute way()
    ->onclick=""
    ->onsubmit=""
  2.property way
  3.add event listeners
LOOPS:
------
  1.for LOOP:
        Syntax:
        for(initialization; condition;)


  2.while LOOPS:
         Syntax:
           while(condition){
            increment/decrement
           }

  3.do while LOOP:
  syntax:
     do{
         increment/decrement;};
         while(condition);
                     
          

  4.dor-in-LOOP:
       ->for-in loop is used to iterate strings,arrays
       ->for-in loop is used to fetch only index's
         syntax:
           for(variable in string/arrays){
             }

  5.for-of-LOOP:
       ->for-of-loop is used to iterate strings,Arrays
       ->for-of-loop is used to fetch only values
       SYNTAX:
        for(variable of string/arrays){


        }
  6.foreach LOOP:
    ->foreach() is the higher order function used to iterate only for arrays
        syntax:
        variableName.forEach((value,index,array)=>{
         })

Array inbult 
------------
    ->map()
    ->filter()
    ->reduce()






















    